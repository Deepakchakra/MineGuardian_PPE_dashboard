"use client";
import type { HelmetData } from "@/hooks/useHelmetData";
import { findCheckpoint, getWorkerCoveragePosition } from "@/lib/mineMap/checkpoints";
export default function HelmetDetails({helmet,onAlert}:{helmet:HelmetData|null;onAlert:(h:HelmetData)=>void}){if(!helmet)return <div className="rounded-xl border border-slate-800 bg-[#071118] p-6 text-xs text-slate-500">Select a helmet to view live details.</div>;const cp=findCheckpoint(helmet.checkpoint.where);const coverage=cp?getWorkerCoveragePosition(cp.id):null;const active=helmet.wifi_rssi!==null||helmet.uptime!==null||helmet.temperature!==null;return <div className="rounded-xl border border-slate-800 bg-[#071118] p-5"><div className="flex items-center justify-between"><div><p className="text-[10px] text-slate-500">SELECTED HELMET</p><h2 className="mt-1 text-lg font-semibold">{helmet.helmetId.replace("_","-").toUpperCase()}</h2></div><button onClick={()=>onAlert(helmet)} className="rounded-md bg-red-600 px-3 py-2 text-[10px] font-semibold text-white hover:bg-red-500">SEND ALERT</button></div><div className="mt-5 grid grid-cols-2 gap-3 text-[10px]"><Info l="Status" v={active?"ACTIVE":"OFFLINE"}/><Info l="Worker" v={helmet.workerName||helmet.workerId||"N/A"}/><Info l="Last checkpoint" v={cp?.name||helmet.checkpoint.where||"N/A"}/><Info l="RFID time" v={helmet.checkpoint.time||"N/A"}/><Info l="Coverage" v={coverage?.to?`${coverage.from.name} → ${coverage.to.name}`:coverage?.from.name||"N/A"}/><Info l="Battery" v={helmet.battery!==null?`${helmet.battery}%`:"N/A"}/><Info l="Signal" v={helmet.wifi_rssi!==null?`${helmet.wifi_rssi} dBm`:"N/A"}/><Info l="Uptime" v={helmet.uptime!==null?`${helmet.uptime}s`:"N/A"}/></div></div>}
function Info({l,v}:{l:string;v:string}){return <div className="border-t border-slate-800 pt-2"><p className="text-slate-500">{l}</p><p className="mt-1 font-medium text-white">{v}</p></div>}
